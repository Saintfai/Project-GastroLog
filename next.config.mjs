/** @type {import('next').NextConfig} */
const nextConfig = {
  // Jika sebelumnya kamu punya pengaturan khusus, masukkan di sini.
  // Jika tidak ada, biarkan kosong seperti ini.
};

export default nextConfig;
